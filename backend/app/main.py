from fastapi import FastAPI\nfrom fastapi.middleware.cors import CORSMiddleware\n\napp = FastAPI()\n\n# CORS middleware configuration\napp.add_middleware(\n    CORSMiddleware,\n    allow_origins=['*'],  # Specifies which domains can access the API\n    allow_credentials=True,\n    allow_methods=['*'],\n    allow_headers=['*'],\n)\n\n@app.get("/health")\ndef health_check():\n    return {"status": "healthy"}\n\n@app.get("/scan")\ndef security_scan():\n    # Implementation for security scanning would go here\n    return {"message": "Security scan initiated"}